<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.user.repository.HobbyMapper">

    <insert id="insertItem" parameterType="com.example.demo.user.entity.Hobby">
        <selectKey keyProperty="id" resultType="Integer" order="BEFORE">
            SELECT MAX(id) +1
              FROM hobby
        </selectKey>
        INSERT INTO user (id, name)
        VALUES (#{id}, #{name})
    </insert>

</mapper>
