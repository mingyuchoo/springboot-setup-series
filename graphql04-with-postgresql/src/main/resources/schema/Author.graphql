type Author {
    id: ID!
    firstName: String
    lastName: String
    books: [Book]
    createdAt: DateTime
    updatedAt: DateTime
}

input CreateAuthorInput {
    firstName: String!
    lastName: String!
}
